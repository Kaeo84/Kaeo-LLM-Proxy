<UserControl x:Class="Kaeo.LlmProxy.VSExtension.Diagnostics.DebugOutputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
             Background="{DynamicResource {x:Static shell:VsBrushes.ToolWindowBackgroundKey}}"
             Foreground="{DynamicResource {x:Static shell:VsBrushes.ToolWindowTextKey}}"
             vsui:ThemedDialogStyleLoader.UseDefaultThemedDialogStyles="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="4,2"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource {x:Static shell:VsBrushes.CommandBarBorderKey}}"
                Background="{DynamicResource {x:Static shell:VsBrushes.CommandBarGradientBeginKey}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ClearButton" Content="Clear" MinWidth="60" Height="22" Click="ClearButton_Click" />
            </StackPanel>
        </Border>

        <TextBox x:Name="OutputBox" Grid.Row="1"
                 IsReadOnly="True"
                 BorderThickness="0"
                 FontFamily="Consolas" FontSize="12"
                 TextWrapping="NoWrap"
                 IsInactiveSelectionHighlightEnabled="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Background="{DynamicResource {x:Static shell:VsBrushes.ToolWindowBackgroundKey}}"
                 Foreground="{DynamicResource {x:Static shell:VsBrushes.ToolWindowTextKey}}" />
    </Grid>
</UserControl>